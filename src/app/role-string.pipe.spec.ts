import { RoleStringPipe } from './role-string.pipe';

describe('RoleStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleStringPipe();
    expect(pipe).toBeTruthy();
  });
});
